# Milestone 0 — feasibility gate

Status: **OPTIONS/EVENT GATE BLOCKED; SPOT TRANSPORT VERIFIED**. This file records the required four
answers; its existence alone does not mean the gate has passed. Deterministic
math, parsing, freshness, reaction, and ledger code exist. A small manually
approved Reality-stock order verified Agent Hub transport, but unattended event
execution remains disabled.

The executable gate is `docs/m0_gate.json`. `reverb.m0.gate_passed()` requires a
`PASSED` artifact, four `VERIFIED` answers, and a matching SHA-256 for every
evidence file. Editing this prose or changing the status string here cannot
unlock execution. The checked artifact is currently `BLOCKED`.

## 1. Option trading hours

DOCUMENTED: Bitget's options rules specify 09:30–16:00 Eastern Time.
[First-party source](https://www.bitget.com/support/articles/12560603889520).
This supports the planned options-before-close / token-after-close design.
OBSERVED metadata and outside-hours option quote: not obtained. A missing API
permission is not evidence of market closure. Holiday and early-close handling
remain unverified and must not be derived from weekdays alone.

DOCUMENTED correction: Bitget announced short calls and short puts on 1 September
2026. Reverb keeps the long-call/long-put branch only; this is a product risk
boundary, not a claim that short options are unavailable.

## 2. Eligibility

DOCUMENTED: Bitget's Stock+ API announcement says API trading requires KYC and
the relevant permissions through the U.S. Stocks section. This is account/product
eligibility, not a separate Stock+ checkbox on the API-key page. API options
quotes additionally require an OPRA market-data card and whitelisting through
Bitget's business representative. [Launch access requirements](https://www.bitget.com/support/articles/12560603890720)
and [current options API docs](https://www.bitget.com/docs/catalog/stock-plus/options-quotes).

OBSERVED on 22 September 2026: a newly created post-upgrade HMAC key
authenticated successfully and a read-only UTA trade request succeeded. The
key exposes `uta_trade` read/write and intentionally has no wallet, withdrawal,
or transfer permission. Account settings/assets return `40014` because this key
does not have UTA management permission; Reverb records that distinction and
uses Agent Hub's read-only `maxOpen` result as the buy-side balance gate rather
than treating unavailable Management metadata as failed Trade authentication.

OBSERVED: Agent Hub filled a manually approved `RCAPRUSDT` limit buy. The
hash-chained receipt is under `evidence/live/20260922T103212Z/`. This verifies
the local machine, UTA Trade key, Reality instrument, and Agent Hub write path;
it was not pre-registered by Reverb and is not earnings-strategy evidence.

OBSERVED: both the protected Stock+ quote and option-expiry routes return
`100001 — U.S. stock trading is not enabled for this account.` This definitively
fails account-specific Stock+ eligibility today. It does not imply that the
product is unavailable to every account. The submission must use the
credential-free replay/public-data path unless Bitget enables U.S. Stocks for
this account.

HISTORICAL OBSERVATION on 21 September 2026: the prior HMAC key authenticated against
`/api/v2/spot/account/info`; Bitget returned `40085`, explicitly confirming that
the account is already in Unified Account mode. With the same credentials and
signature mechanism, protected v3 account, trade, and Stock+ routes all returned
`40012`, including read-only unfilled-order and fill requests covered by the
configured UTA trade scope. The local clock was within two seconds of Bitget's
public server time. This is evidence of a v3 key activation/authentication
blocker, not a malformed key and not a Stock+ eligibility result. Stock+ and
OPRA entitlement remain untested behind that blocker.

## Reality-stock spot fees

DOCUMENTED: Bitget's [September fee update](https://www.bitget.com/support/articles/12560603893944)
keeps a published base fee for Reality-token spot trading and publishes lower
VIP/PRO tiers. Its [tier and routing notice](https://www.bitget.com/support/articles/12560603891318)
says regular U.S. market-day StockRoute orders use the taker rate; weekend and
holiday internal matching uses its maker/taker rules.

OBSERVED: the public `fee-group` capture at
`evidence/runs/20260923T052448.226811Z-c0ad535b/005-fee-group.body` contains
the `rtoken` group label and group-level fee tiers. It does not expose which
account-specific rate is active. The manually approved RCAPR receipt records
an effective fee of 5 basis points on that one fill; it was not a Reverb
earnings trade and is not proof of every symbol/session's applied rate.

UNVERIFIED for this account: `GET /api/v3/account/fee-rate` returns the exact
symbol-specific maker/taker rate, but [Bitget requires UTA Management read
permission](https://www.bitget.com/api-doc/uta/account/Get-Account-Fee-Rate).
The configured key has UTA Trade only, so Agent Hub did not return its fee-rate
section. No withdrawal or transfer permission is needed or requested. The
published schedule and one fill are evidence; the per-account API lookup is
not. Until the lookup or a separately validated conservative runtime fee rule
is in place, Reverb does not approve a live spot order.

## 3. Tradeable intersection

DOCUMENTED: `/api/v3/market/instruments?category=SPOT` supplies Reality identity
using `isReality: "yes"`; `symbolType: "stock"` alone is insufficient.
[Market API](https://www.bitget.com/docs/catalog/market/market-data).
The probe resolves rTokens at runtime and checks names against the stock-token
table in the configured first-party weekend-support announcement. This is a
dated, documented candidate list, not proof of present executable liquidity.

DOCUMENTED and OBSERVED: `/api/v3/reality/market/stock-info` now supplies live
`tradingPeriod` and `weekendTradable` fields. The 22 September public capture
verified 90 online Reality symbols for both after-hours and weekend trading.
This resolves the 24/7 side of the universe at runtime without hardcoding the
announcement list. It does not resolve which of those 90 have accessible
options, so the actual A ∩ B intersection remains unverified.

DOCUMENTED: Stock+ exposes option expiry and chain endpoints in
[the current public documentation](https://www.bitget.com/docs/catalog/stock-plus/options-quotes).
ASSUMED/UNVERIFIED: their accessible runtime universe and A ∩ B. An unavailable
chain is recorded as unknown, never as an empty intersection.

## 4. The earnings-time book

NOT MEASURED: three earnings-day books at 16:05 New York and their regular-hours
medians. The public probe captures books with exchange and receipt timestamps,
spread and displayed depth; these are diagnostic snapshots only. Historical
candles cannot reconstruct a historical order book or execution.

OBSERVED on 23 September 2026: Reverb confirmed `RCOSTUSDT` in the live Reality
instrument list and `stock-info` reported `tradingPeriod` containing
`after_hours`, with `weekendTradable: no`. This establishes current symbol
identity and the published session flags, not event-time liquidity or weekend
availability.

The dedicated `BITGET_READ_*` key now authenticates as read-only and reports
both `uta_trade=true` and `uta_mgt=true`. That resolves the key-scope question.
The account-scoped `/api/v3/account/reality-orderbook` and
`/api/v3/account/reality-fills` routes nevertheless return HTTP 400 / Bitget
`40025`. Sanitized results are hash-chained in
[`evidence/reality-access/ledger.jsonl`](../evidence/reality-access/ledger.jsonl).

Direct comparison with the earlier EXNIGHT build found the important routing
difference: EXNIGHT successfully captured two-sided Reality depth through the
public UTA v3 `/api/v3/market/orderbook?category=SPOT` endpoint. That public
route is useful as a diagnostic, but it is not the raw Reality feed required by
the Costco specification. The Costco recorder therefore requires candles plus
the authenticated account-scoped Reality book. Public SPOT book/fills remain
separately labelled diagnostics, and platform fills are optional provenance
inputs; every failure is recorded explicitly.

The latest probe returned an empty public RCOST book and a 50-by-50 public
RNVDA book. An empty RCOST book at a particular instant is a measured liquidity
result, not an authentication failure. The forward run is `INCOMPLETE` if the
required candle or raw Reality-book capture interval is missing; Reality
platform fills are attempted **if accessible**, as specified, and are never
fabricated or silently substituted.

ASSUMED/UNVERIFIED: economic execution feasibility. Limit orders cannot cure
missing liquidity; no live execution is permitted on these snapshots alone.

The historical NVIDIA replay is separate evidence: it verifies a public
post-release candle window, not this live 16:05 book gate. It cannot change the
gate answer or support a fill claim.

## Calendar probe

**OBSERVED:** the configured public Nasdaq earnings endpoint accepts one `date`
query per day and returns rows under `data.rows`. A row can contain a date but
`time` can be `time-not-supplied`; it is not safe to turn that into a trade time
implicitly. Reverb therefore requires `config/calendar.json` to provide an
explicit `default_event_time_et` before a date-only event can schedule work.
The configured fallback is now explicitly `16:05 ET`; tool results label it
`configured_default_assumption` rather than source time. This is calendar
plumbing, not Bitget product evidence, and the reporting-time assumption remains
**ASSUMED** until verified from a first-party event source.

DOCUMENTED on 23 September 2026: the current-week calendar's only matched
24/7 Reality candidate, BlackBerry (BB), is not a suitable after-close test. The
[issuer's investor-relations page](https://investors.blackberry.com/) lists its
FY2027 Q2 results event for 24 September at 8:00 AM EDT. The Nasdaq row's
unresolved time is therefore not allowed to drive a 16:05 reaction schedule.
This candidate does not satisfy the earnings-time book sample.

## Raw evidence and reproduction

`python3 scripts/probe.py capture` stores raw public HTTP response bodies and
appends their metadata to a hash-chained evidence ledger. Every run snapshots
the exact config. `python3 scripts/probe.py report` verifies hashes and computes
[measurements](measurements.md) from the most recent captured run. Failures stay
in the ledger and make capture exit nonzero; successful independent requests
are retained for diagnosis. No authenticated response or secret is published.

## Conditions to advance

1. The submission has selected the documented spot-only fallback. Stock+ remains
   an explicitly blocked extension.
2. For the deferred options extension, enumerate the actual chain intersection
   and verify bid/ask, expiry, multiplier, underlying identity, and fees.
3. Capture the specified earnings-time book sample and regular-hours comparison,
   or obtain an explicit change to that mandatory build gate.
4. Carry unresolved rules into the README and every affected decision.

## Runtime safety blockers added after the initial gate

The spot fallback will not approve or submit an unattended event trade until
the reaction order has an explicit side, quantity, limit price and budget, and
Bitget's runtime precision, minimum, price-band and Agent Hub buying-power
fields have been validated for that symbol. The deferred option branch also
requires a calibrated post-event volatility scenario. These are deliberately
fail-closed requirements, not defaults.
